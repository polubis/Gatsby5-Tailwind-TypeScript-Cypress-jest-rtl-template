name: Typecheck
on:
  pull_request:
    branches: ['main']

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Typecheck
        run: |
          npm install --legacy-peer-deps
          npm run typecheck
