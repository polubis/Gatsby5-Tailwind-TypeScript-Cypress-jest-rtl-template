name: E2E
on:
  pull_request:
    branches: ['main']

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: E2E
        run: |
          npm install --legacy-peer-deps
          npm run test:e2e:ci
